<div class="form-group">
    <label for="{{name}}">
        {{label}}
    </label>

    <input type="{{type || 'text'}}" name="{{name}}" id="{{name}}" class="{{cls}}" value="{{value || ''}}">
</div>

@inputError(name)
    @each(message in messages)
        <p>{{message}}</p>
    @end
@end